Задания выполняются по вариантам, вычисляемым по формуле
номер_по_списку % 5 +1
На каждую задачу составляется блок-схема; программа пишется на языке С/С++ в двух вариантах оформления ввода-вывода: потоковый (iostream) и форматный (stdio). Таким образом, к каждой задаче составляется одна блок-схема и две программы.